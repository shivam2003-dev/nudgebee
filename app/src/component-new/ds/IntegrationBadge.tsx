/**
 * IntegrationBadge — DS rename of legacy IconTextBadge.
 * Spec: app/design-system/primitives/data-display/integration-badge.html
 *
 * Preset list (slack, ms_teams, google_chat, email, jira, servicenow, pagerduty)
 * and full prop API are preserved. New code should import from this path.
 */
export { default } from '@components1/common/IconTextBadge';
