apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-server.fullname" . }}
  labels:
    {{- include "ticket-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticket-server.selectorLabels" . | nindent 4 }}
