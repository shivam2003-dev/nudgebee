apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ticket-server.fullname" . }}-test-connection"
  labels:
    {{- include "ticket-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ticket-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
