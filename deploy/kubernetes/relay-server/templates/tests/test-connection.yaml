apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "relay-server.fullname" . }}-test-connection"
  labels:
    {{- include "relay-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "relay-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
