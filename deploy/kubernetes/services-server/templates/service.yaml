apiVersion: v1
kind: Service
metadata:
  name: {{ include "services-server.fullname" . }}
  labels:
    {{- include "services-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "services-server.selectorLabels" . | nindent 4 }}
