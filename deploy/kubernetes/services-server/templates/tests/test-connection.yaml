apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "services-server.fullname" . }}-test-connection"
  labels:
    {{- include "services-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "services-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
