apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-collector-server.fullname" . }}
  labels:
    {{- include "cloud-collector-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-collector-server.selectorLabels" . | nindent 4 }}
