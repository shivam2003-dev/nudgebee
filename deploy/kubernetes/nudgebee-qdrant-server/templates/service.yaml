apiVersion: v1
kind: Service
metadata:
  name: {{ include "nudgebee-qdrant-server.fullname" . }}
  labels:
    {{- include "nudgebee-qdrant-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "nudgebee-qdrant-server.selectorLabels" . | nindent 4 }}
