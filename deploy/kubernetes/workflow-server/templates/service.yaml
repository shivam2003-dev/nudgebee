apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-server.fullname" . }}
  labels:
    {{- include "workflow-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workflow-server.selectorLabels" . | nindent 4 }}
