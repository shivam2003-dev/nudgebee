user_prompt: What is the SQL query to list all events where the subject name contains
  only numbers?
expected_output:
- SELECT id, title, subject_name FROM events WHERE subject_name ~ '^[0-9]+$';
tags:
- command
- events
