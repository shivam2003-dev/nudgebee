user_prompt: What is the SQL query to count closed issues per aggregation key?
expected_output:
- SELECT aggregation_key, COUNT(*) AS closed_issue_count FROM events WHERE status='CLOSED'
  AND finding_type='issue' GROUP BY aggregation_key ORDER BY closed_issue_count DESC;
tags:
- command
- events
