user_prompt: What is the SQL query to find events related to HorizontalPodAutoscalers
  (HPA)?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, subject_namespace   FROM
  events   WHERE subject_type = 'horizontalpodautoscaler';
tags:
- command
- events
