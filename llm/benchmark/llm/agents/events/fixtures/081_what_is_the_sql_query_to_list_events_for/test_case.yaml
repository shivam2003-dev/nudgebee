user_prompt: What is the SQL query to list events for subject type deployment?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_node FROM events WHERE subject_type
  = 'deployment';
- SELECT * FROM events WHERE subject_type = 'deployment';
tags:
- command
- events
