user_prompt: What is the SQL query to archive events older than 90 days by moving
  them to events_archive?
expected_output:
- INSERT INTO events_archive SELECT * FROM events WHERE starts_at < NOW() - INTERVAL
  '90 days';
tags:
- command
- events
