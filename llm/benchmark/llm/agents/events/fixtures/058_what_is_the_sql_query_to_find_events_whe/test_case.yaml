user_prompt: What is the SQL query to find events where access keys were created?
expected_output:
- SELECT id, title, starts_at, priority, subject_name, source   FROM events   WHERE
  aggregation_key = 'CreateAccessKey';
- SELECT * FROM events   WHERE aggregation_key = 'CreateAccessKey';
tags:
- command
- events
