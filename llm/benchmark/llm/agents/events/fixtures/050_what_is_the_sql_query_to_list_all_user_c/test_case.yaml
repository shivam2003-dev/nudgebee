user_prompt: What is the SQL query to list all user creation events?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, source   FROM events   WHERE
  aggregation_key = 'CreateUser';
- SELECT * FROM events   WHERE aggregation_key = 'CreateUser';
tags:
- command
- events
