user_prompt: What is the SQL query to list events where user policies were attached?
expected_output:
- SELECT id, title, starts_at, priority, subject_name, description, source   FROM
  events   WHERE aggregation_key = 'AttachUserPolicy';
- SELECT * FROM events   WHERE aggregation_key = 'AttachUserPolicy';
tags:
- command
- events
