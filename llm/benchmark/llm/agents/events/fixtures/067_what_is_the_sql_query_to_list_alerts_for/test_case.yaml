user_prompt: What is the SQL query to list alerts for Kubelet being down?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_node   FROM events   WHERE
  aggregation_key = 'KubeletDown';
- SELECT * FROM events   WHERE aggregation_key = 'KubeletDown';
tags:
- command
- events
