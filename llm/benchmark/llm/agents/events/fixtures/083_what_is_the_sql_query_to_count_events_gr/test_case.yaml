user_prompt: What is the SQL query to count events grouped by priority and order by
  the highest counts?
expected_output:
- SELECT priority, COUNT(*) AS event_count FROM events GROUP BY priority ORDER BY
  event_count DESC;
tags:
- command
- events
