user_prompt: What is the SQL query to show all anomaly detection events?
expected_output:
- SELECT id, title, starts_at, status, priority, description, source   FROM events   WHERE
  aggregation_key = 'Anomaly';
- SELECT * FROM events   WHERE aggregation_key = 'Anomaly';
tags:
- command
- events
