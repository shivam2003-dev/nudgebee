user_prompt: What is the SQL query to show events where EC2 instances were launched?
expected_output:
- SELECT id, title, starts_at, status, priority, description, source   FROM events   WHERE
  aggregation_key = 'RunInstances';
- SELECT * FROM events   WHERE aggregation_key = 'RunInstances';
tags:
- command
- events
