user_prompt: What is the SQL query to find events related to backup job completions?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, description   FROM
  events   WHERE aggregation_key = 'BackupJobCompleted';
- SELECT * FROM events   WHERE aggregation_key = 'BackupJobCompleted';
tags:
- command
- events
