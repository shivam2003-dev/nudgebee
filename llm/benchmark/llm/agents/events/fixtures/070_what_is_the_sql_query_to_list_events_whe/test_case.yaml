user_prompt: What is the SQL query to list events where roles were attached to policies?
expected_output:
- SELECT id, title, starts_at, priority, subject_name, description, source   FROM
  events   WHERE aggregation_key = 'AttachRolePolicy';
- SELECT * FROM events   WHERE aggregation_key = 'AttachRolePolicy';
tags:
- command
- events
