user_prompt: What is the SQL query to list events where volumes were deleted?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, source   FROM events   WHERE
  aggregation_key = 'DeleteVolume';
- SELECT * FROM events   WHERE aggregation_key = 'DeleteVolume';
tags:
- command
- events
