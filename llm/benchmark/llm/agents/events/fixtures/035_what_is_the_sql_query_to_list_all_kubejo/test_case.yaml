user_prompt: What is the SQL query to list all 'KubeJobFailed' events?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, subject_namespace   FROM
  events   WHERE aggregation_key = 'KubeJobFailed';
tags:
- command
- events
