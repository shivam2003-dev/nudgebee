user_prompt: List all events that occurred on nodes which have experienced a 'KubeNodeNotReady'
  event.
expected_output:
- SELECT * FROM events WHERE subject_node IN (SELECT DISTINCT subject_node FROM events
  WHERE aggregation_key = 'KubeNodeNotReady');
tags:
- command
- events
