user_prompt: What is the SQL query to list all events that are currently closed?
expected_output:
- SELECT id, title, starts_at, ends_at, priority, subject_name   FROM events   WHERE
  status = 'CLOSED';
- SELECT * FROM events   WHERE status = 'CLOSED';
tags:
- command
- events
