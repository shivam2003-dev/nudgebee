user_prompt: What is the SQL query to find events where MFA devices were enabled?
expected_output:
- SELECT id, title, starts_at, priority, subject_name, source   FROM events   WHERE
  aggregation_key = 'EnableMFADevice';
- SELECT * FROM events   WHERE aggregation_key = 'EnableMFADevice';
tags:
- command
- events
