user_prompt: What is the SQL query to show all events that started in the last hour?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name   FROM events   WHERE
  starts_at >= NOW() - INTERVAL '1 hour';
- SELECT * FROM events   WHERE starts_at >= NOW() - INTERVAL '1 hour';
tags:
- command
- events
