user_prompt: What is the SQL query to find events with the exact title 'Node Not Ready'?
expected_output:
- SELECT id, starts_at, status, priority, subject_node   FROM events   WHERE title
  = 'Node Not Ready';
tags:
- command
- events
