user_prompt: What is the SQL query to find alerts for PostgreSQL being down?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name   FROM events   WHERE
  aggregation_key = 'PostgreSQLDown';
- SELECT * FROM events   WHERE aggregation_key = 'PostgreSQLDown';
tags:
- command
- events
