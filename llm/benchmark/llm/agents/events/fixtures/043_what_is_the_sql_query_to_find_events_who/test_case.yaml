user_prompt: What is the SQL query to find events whose fingerprint appears more than
  once (duplicates based on fingerprint)?
expected_output:
- SELECT fingerprint, COUNT(*) AS occurrences   FROM events   GROUP BY fingerprint   HAVING
  COUNT(*) > 1;
tags:
- command
- events
