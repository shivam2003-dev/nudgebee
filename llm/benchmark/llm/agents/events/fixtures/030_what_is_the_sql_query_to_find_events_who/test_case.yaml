user_prompt: What is the SQL query to find events whose description contains 'timeout'?
expected_output:
- SELECT id, title, description, starts_at, status, priority   FROM events   WHERE
  description LIKE '%timeout%';
- SELECT * FROM events   WHERE description LIKE '%timeout%';
tags:
- command
- events
