user_prompt: What is the SQL query to list all 'ServiceDown' events?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name   FROM events   WHERE
  aggregation_key = 'ServiceDown';
- SELECT * FROM events   WHERE aggregation_key = 'ServiceDown';
tags:
- command
- events
