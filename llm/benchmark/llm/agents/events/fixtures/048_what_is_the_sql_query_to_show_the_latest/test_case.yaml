user_prompt: What is the SQL query to show the latest event overall based on start
  time?
expected_output:
- SELECT id, title, starts_at, status, priority, aggregation_key   FROM events   ORDER
  BY starts_at DESC   LIMIT 1;
- SELECT * FROM events   ORDER BY starts_at DESC   LIMIT 1;
tags:
- command
- events
