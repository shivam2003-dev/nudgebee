user_prompt: What is the SQL query to count the number of events for each priority
  level?
expected_output:
- SELECT priority, COUNT(*) AS event_count   FROM events   GROUP BY priority   ORDER
  BY event_count DESC;
tags:
- command
- events
