user_prompt: What is the SQL query to find events where subject name starts with 'api-'?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_name, subject_namespace   FROM
  events   WHERE subject_name LIKE 'api-%';
tags:
- command
- events
