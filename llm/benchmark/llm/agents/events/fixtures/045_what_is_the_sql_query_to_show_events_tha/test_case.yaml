user_prompt: What is the SQL query to show events that ended in the last hour?
expected_output:
- SELECT id, title, starts_at, ends_at, status, priority   FROM events   WHERE ends_at
  >= NOW() - INTERVAL '1 hour';
- SELECT * FROM events   WHERE ends_at >= NOW() - INTERVAL '1 hour';
tags:
- command
- events
