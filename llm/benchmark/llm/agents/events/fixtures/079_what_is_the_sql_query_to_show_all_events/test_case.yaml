user_prompt: What is the SQL query to show all events related to a nginx-pod?
expected_output:
- SELECT id, title, starts_at, status, priority, subject_node FROM events WHERE regarding_name
  = 'nginx-pod';
- SELECT * FROM events WHERE regarding_name = 'nginx-pod';
tags:
- command
- events
