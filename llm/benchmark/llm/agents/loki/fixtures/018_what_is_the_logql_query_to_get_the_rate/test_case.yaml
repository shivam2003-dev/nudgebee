user_prompt: What is the LogQL query to get the rate of logs per minute for the auth
  service from kubernetes-logs?
expected_output:
- rate({job="kubernetes-logs", service="auth"}[1m])
- sum(rate({namespace="production", service="auth"}[1m]))
tags:
- command
- loki
