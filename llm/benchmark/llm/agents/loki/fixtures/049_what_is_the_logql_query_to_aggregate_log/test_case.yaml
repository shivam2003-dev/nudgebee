user_prompt: What is the LogQL query to aggregate log counts by namespace and app
  using the kubernetes-logs job over 5 minutes?
expected_output:
- sum by (namespace, app) (rate({job="kubernetes-logs"}[5m]))
- count_over_time({job!=""}[10m]) by (namespace, app)
tags:
- command
- loki
