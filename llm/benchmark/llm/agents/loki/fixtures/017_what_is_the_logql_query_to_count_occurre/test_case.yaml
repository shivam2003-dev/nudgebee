user_prompt: What is the LogQL query to count occurrences of timeout in the kubernetes-logs
  job over the last 1 hour?
expected_output:
- count_over_time({job="kubernetes-logs"} |= "timeout" [1h])
- sum(count_over_time({namespace="default"} |= "timeout" [1h]))
tags:
- command
- loki
