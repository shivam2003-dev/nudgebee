user_prompt: What is the LogQL query to get the maximum memory usage reported in the
  worker logs over the last 1 hour?
expected_output:
- max_over_time({app="worker"} | json | unwrap memory_usage_mb [1h])
- max(rate({job="java-app"} | logfmt | unwrap heap_size [5m]))
tags:
- command
- loki
