user_prompt: what is the promql query for the total number of persistent volumes in
  the cluster?
expected_output:
- count(kube_persistentvolume_info)
- sum(kube_persistentvolume_info)
tags:
- command
- promql
