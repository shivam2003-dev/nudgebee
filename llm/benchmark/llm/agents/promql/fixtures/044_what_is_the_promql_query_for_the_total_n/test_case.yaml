user_prompt: what is the promql query for the total number of running containers across
  the cluster?
expected_output:
- sum(kube_pod_container_status_running)
- count(kube_pod_container_status_running)
tags:
- command
- promql
