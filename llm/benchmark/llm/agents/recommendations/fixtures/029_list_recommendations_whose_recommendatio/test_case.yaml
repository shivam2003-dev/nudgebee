user_prompt: List recommendations whose 'recommendation' text contains the word 'upgrade'.
expected_output:
- SELECT namespace, name, category, recommendation from recommendation_view WHERE
  recommendation ILIKE '%upgrade%';
tags:
- command
- recommendations
