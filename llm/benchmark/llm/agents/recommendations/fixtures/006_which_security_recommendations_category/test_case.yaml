user_prompt: Which security recommendations (category 'Security') are currently open
  and have high or critical severity?
expected_output:
- SELECT rule_name, namespace, name, severity from recommendation_view WHERE category
  = 'Security' AND status = 'Open' AND severity IN ('High', 'Critical');
tags:
- command
- recommendations
