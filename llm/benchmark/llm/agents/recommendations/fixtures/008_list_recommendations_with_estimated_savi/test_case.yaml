user_prompt: List recommendations with estimated savings greater than $100, ordered
  by saving amount (highest first).
expected_output:
- SELECT namespace, name, category, estimated_saving from recommendation_view WHERE
  estimated_saving > 100 ORDER BY estimated_saving DESC;
tags:
- command
- recommendations
