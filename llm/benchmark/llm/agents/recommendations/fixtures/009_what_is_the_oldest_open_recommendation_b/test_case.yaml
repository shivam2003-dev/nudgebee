user_prompt: What is the oldest 'Open' recommendation based on its creation date?
expected_output:
- SELECT namespace, name, recommendation, created_at from recommendation_view WHERE
  status = 'Open' ORDER BY created_at ASC LIMIT 1;
tags:
- command
- recommendations
