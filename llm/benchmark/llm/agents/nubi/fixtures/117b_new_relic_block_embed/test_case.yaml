user_prompt: "Show me traces for do-fra1-tomer and do-fra1-shahal clusters"

expected_output: 'The answer should include something similar to << { "type": "traces-summary", "tool_name": "newrelic_execute_nrql_query", "random_key": "<alphanumeric_characters>" } >>. Both tool_name and random_key MUST absolutely be present and the same as mentioned here.'

tags:
  - chain-of-causation
  - traces
  - newrelic
  - network

skip: true
skip_reason: "Requires New Relic (NEW_RELIC_ACCOUNT_ID, NEW_RELIC_API_KEY)"
