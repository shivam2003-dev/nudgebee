user_prompt: "list all clusterroles in the cluster"
expected_output:
  - user is told they don't have access to list clusterroles
  - should indicate that permissions need to be configured to access clusterroles
before_test: ./setup_restricted_access.sh
after_test: ./cleanup_restricted_access.sh
test_env_vars:
  KUBECONFIG: "$TMPDIR/Nubi-test-28-permissions/restricted-kubeconfig"
tags:
  - easy
  - transparency
