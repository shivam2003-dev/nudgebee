# similar to eval 80 - not sure we need both
user_prompt: "What is the issue with redis?"
expected_output: Redis pods are not running because their PVC cannot bind because the StorageClass "standard-v2" does not exist

tags:
  - hard
  - kubernetes
before_test: |
  kubectl create namespace app-24b || true
  kubectl label namespace app-24b benchmark=true --overwrite || true
  kubectl apply -f ./manifest.yaml -n app-24b
after_test: |
  kubectl delete -f ./manifest.yaml -n app-24b
  kubectl delete namespace app-24b || true
