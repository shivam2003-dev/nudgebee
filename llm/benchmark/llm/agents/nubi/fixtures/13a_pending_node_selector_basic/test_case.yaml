user_prompt: "What is the issue with user-profile-import"
expected_output: "node selector mismatch"
before_test: |
  kubectl create namespace app-13a || true
  kubectl label namespace app-13a benchmark=true --overwrite || true
  kubectl apply -f ./manifest.yaml -n app-13a
  sleep 30
after_test: |
  kubectl delete -f ./manifest.yaml -n app-13a || true
  kubectl delete namespace app-13a || true
tags:
  - easy
  - kubernetes
