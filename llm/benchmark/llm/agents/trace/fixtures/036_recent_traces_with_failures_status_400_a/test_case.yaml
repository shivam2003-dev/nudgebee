user_prompt: Recent traces with failures (status >= 400) across all services.
expected_output:
- SELECT * FROM traces_view WHERE http_status_code >= 400 ORDER BY timestamp DESC
  LIMIT 50;
- SELECT timestamp, workload_name, workload_namespace, resource, destination_workload_name,
  destination_workload_namespace, http_status_code, duration_ns FROM traces_view WHERE
  http_status_code >= 400 ORDER BY timestamp DESC LIMIT 50;
tags:
- command
- trace
