user_prompt: Recent traces with high latency (> 1s) in 'nudgebee' namespace.
expected_output:
- SELECT * FROM traces_view WHERE workload_namespace = 'nudgebee' AND duration_ns
  > 1000 AND timestamp >= now() - interval 1 hour ORDER BY timestamp DESC LIMIT 50;
- SELECT timestamp, workload_name, workload_namespace, resource, destination_workload_name,
  destination_workload_namespace, http_status_code, duration_ns FROM traces_view WHERE
  workload_namespace = 'nudgebee' AND duration_ns > 1000 AND timestamp >= now() -
  interval 1 hour ORDER BY timestamp DESC LIMIT 50;
tags:
- command
- trace
