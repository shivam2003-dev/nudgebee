user_prompt: Get API traces originating from 'nudgebee' namespace.
expected_output:
- SELECT * FROM traces_view WHERE workload_namespace = 'nudgebee' ORDER BY timestamp
  DESC LIMIT 100;
- SELECT timestamp, workload_name, workload_namespace, resource, destination_workload_name,
  destination_workload_namespace, http_status_code, duration_ns FROM traces_view WHERE
  workload_namespace = 'nudgebee' ORDER BY timestamp DESC LIMIT 100;
tags:
- command
- trace
