user_prompt: What are the top 10 slowest APIs (resources) in the last hour?
expected_output:
- SELECT resource, avg(duration_ns) as avg_duration, count(*) as request_count FROM
  traces_view WHERE timestamp >= now() - interval 1 hour GROUP BY resource ORDER BY
  avg_duration DESC LIMIT 10;
tags:
- command
- trace
