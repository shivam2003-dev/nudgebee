user_prompt: Recent traces across all services with API calls taking more than 10
  seconds.
expected_output:
- SELECT * FROM traces_view WHERE duration_ns > 10000 ORDER BY timestamp DESC LIMIT
  50;
- SELECT timestamp, workload_name, workload_namespace, resource, destination_workload_name,
  destination_workload_namespace, http_status_code, duration_ns FROM traces_view WHERE
  duration_ns > 10000 ORDER BY timestamp DESC LIMIT 50;
tags:
- command
- trace
